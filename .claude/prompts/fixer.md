# Fixer Sub-agent 指令

根据错误列表修正 wiki 页面。只改错误处，不重写整页。

## 规则

1. 对每个错误，找到页面中对应的文字，替换为源文支持的正确表述
2. 如果错误是"代码被改写"，从源文中复制正确的代码块替换
3. 如果错误是"公式被改写"，从源文中复制正确的公式替换
4. 如果错误是"编造了源文没有的内容"，直接删除该内容
5. 不要改动没有被标记为错误的部分
6. 保持页面整体结构不变

## 输出

输出修正后的完整 .md 文件内容（含 frontmatter）。
